#include <iostream>
#include <string>
using namespace std;

int main() {
  string line;
  cin >> line;
  for(int i = 0; i < line.length()-2; i++) {
    if(line[i] == ':' || line[i] == ';') {
      if(line[i+1] == '-' 
    }

  }

}
