ios_base::sync_with_stdio(0); //cin becomes faster, makes it not thread safe
                              //with c io, so either use cin/cout or printf
cout << std::setprecision(2); // number of decimals of precision
